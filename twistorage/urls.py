from django.conf.urls.defaults import *

urlpatterns = patterns('',
    
    # twistorage urls - just used to view a file (or a thumb?)
    # url(r'^(?P<path>.*)$', 'twistorage.views.download', name = "tn_download"),

)
